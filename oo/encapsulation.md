# Encapsulation

Encapsulation bundles data and methods that operate on that data within a single unit (e.g., a class), restricting direct access to some of an object's internal state.

## When to use

- To hide internal implementation details from external code
- To protect an object's integrity by controlling how its data is accessed or mutated
- To reduce coupling between components by exposing only a well-defined interface
