# Inheritance

Inheritance is a mechanism where a new class derives properties and behavior from an existing class ("is-a" relationship), promoting code reuse and establishing a hierarchical relationship.

## When to use

- When a genuine "is-a" relationship exists between classes
- To share common behavior across related classes without duplication
- When you expect the hierarchy to remain stable over time
- To leverage polymorphic behavior through base class references
